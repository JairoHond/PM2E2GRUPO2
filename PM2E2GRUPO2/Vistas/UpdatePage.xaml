<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PM2E2GRUPO2.Vistas.UpdatePage"
             Title="Actualizar Registro">

    <StackLayout>
        <!-- Agrega aquí tus campos de edición, por ejemplo: -->
        <Entry Placeholder="Desc" Text="{Binding descripcion}" />
        <Entry Placeholder="Lat" Text="{Binding latitud}" />
        <Entry Placeholder="Lot" Text="{Binding longitud}" />
        <!-- Agrega más campos según sea necesario -->

        <!-- Agrega un botón para guardar los cambios -->
    </StackLayout>
</ContentPage>